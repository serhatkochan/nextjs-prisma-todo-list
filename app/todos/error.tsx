"use client"

// "use client" ile client tarafında çalışmasını sağlıyorruz bu componentin.
// bir hata çıktığında buraya girecek.
const Error = () => {
    return (
        <h1>
            Error
        </h1>
    );
};

export default Error;